-- Central busted configuration for the repository test layout.
--
-- Support files live under Testing/Support, while specs are grouped by domain
-- under Testing/Tests. The runner points busted at this config so local runs
-- and CI share identical discovery behavior.
return {
  default = {
    helper = "Testing/Support/bootstrap.lua",
    pattern = "_spec",
    verbose = true,
  },
}